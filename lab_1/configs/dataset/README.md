# Dataset Config

Файл `data/visdrone_yolo/dataset.yaml` создается автоматически после запуска:

```bash
.venv/bin/python src/download_visdrone.py
```

Эксперименты из `configs/experiments/` используют именно этот локальный `dataset.yaml`.
