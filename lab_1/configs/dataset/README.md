# Dataset Config

Файл `data/brain_tumor/dataset.yaml` создается автоматически после запуска:

```bash
.venv/bin/python src/download_brain_tumor.py
```

Эксперименты из `configs/experiments/` используют именно этот локальный `dataset.yaml`.
